---
name: commit
description: Git commit workflow agent. Use when the user asks to "commit", "stage and commit", "group and commit", or wants to organize changes into logical commit batches.
agent: commit-workflow
---

Use the commit-workflow agent to analyze all uncommitted changes, group them into logical batches, and create clean atomic commits following conventional commit standards.
