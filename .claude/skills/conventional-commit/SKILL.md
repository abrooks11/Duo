---
name: conventional-commit
description: Git commit planning skill. Use when the user asks to "group changes", "plan commits", or wants uncommitted changes organized into logical commit batches with drafted messages.
---

Use the commit-workflow agent to analyze all uncommitted changes, group them into logical batches, and draft conventional commit messages for each group. The agent does not stage or commit anything — it only produces a plan for the user to act on.
